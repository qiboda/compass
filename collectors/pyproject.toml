[project]
name = "compass-scripts"
version = "0.1.0"
description = "Compass data pipeline helper scripts"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.15",
    "tenacity>=9.1",
]

[dependency-groups]
dev = []
