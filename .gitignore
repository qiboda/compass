/target
compass.db
compass.duckdb
logs/
*.log
investment_data/
compass_data/
.dolt/
/data
parquet_data/
*.wal

# Manual worktree storage
.worktrees/

# Python collectors
collectors/.venv/
collectors/__pycache__/
collectors/*.csv
# name_en_mapping.csv is a version-tracked static mapping table (epic #266) —
# unlike fetched CSV artifacts it ships with the repo.
!collectors/name_en_mapping.csv
collectors/*.state.json
__pycache__/
collectors/.coverage

# Saved output files (backtest results, exports) — not tracked
saved/
