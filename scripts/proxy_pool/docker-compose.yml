# Local proxy_pool + Redis for the collectors proxy trial (issue #287).
# proxy_pool now builds from the local patched Dockerfile (issue #290).
#
# Usage:
#   docker compose -f scripts/proxy_pool/docker-compose.yml up -d --build
#   # proxy_pool API: http://127.0.0.1:5010
#   docker compose -f scripts/proxy_pool/docker-compose.yml down
services:
  proxy_redis:
    image: redis:7-alpine
    container_name: proxy_redis
    restart: unless-stopped
    # Keepalive / fetch_freeproxy default to redis://@127.0.0.1:6379/0;
    # publish loopback-only so the host-side feeder can reach Redis (issue #296).
    ports:
      - "127.0.0.1:6379:6379"
    command: ["redis-server", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  proxy_pool:
    build: .
    container_name: proxy_pool
    restart: unless-stopped
    depends_on:
      proxy_redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:5010:5010"
    # The published image's default ENTRYPOINT invokes `bash`, but the image
    # (Alpine-based) does not contain bash.  Start the Python server/scheduler
    # directly with `sh` instead.
    entrypoint: ["/bin/sh", "-c"]
    command: ["python proxyPool.py server & python proxyPool.py schedule & wait"]
    environment:
      DB_CONN: "redis://@proxy_redis:6379/0"
